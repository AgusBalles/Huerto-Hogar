# Example environment variables for MySQL
DB_HOST=localhost
DB_PORT=3306
DB_NAME=huerto
DB_USER=huerto_user
DB_PASSWORD=secret_pass

# If you want Spring Boot to load the MySQL profile by default when running locally:
SPRING_PROFILES_ACTIVE=mysql
